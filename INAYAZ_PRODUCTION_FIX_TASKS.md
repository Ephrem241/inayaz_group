# INAYAZ Production Fix — Task Checklist

Tracks every numbered issue from the production-readiness fix request against affected files, planned change, implementation status, and verification result. Updated as each phase lands — see `INAYAZ_PRODUCTION_FIX_REPORT.md` (Phase J) for the narrative writeup once complete.

Status legend: ⬜ Not started · 🔶 In progress · ✅ Done & verified

| # | Issue | Affected files | Planned change | Status | Verification |
|---|---|---|---|---|---|
| 2 | Localhost SEO URLs in production metadata | `src/constants/site.ts`, `.env.local`, `.env.example`, `src/app/layout.tsx`, `sitemap.ts`, `robots.ts`, `OrganizationJsonLd.tsx`, `Breadcrumbs.tsx`, `ArticleJsonLd.tsx`, `ProjectJsonLd.tsx` (new), `e2e/seo-guards.spec.ts` (new) | Fix stray `.env.local` value, change code fallback to the Vercel URL, add a Playwright guard test asserting no `localhost`/`127.0.0.1` anywhere in rendered output | ✅ | `e2e/seo-guards.spec.ts` 6/6 passing (canonical, OG, JSON-LD, sitemap.xml, robots.txt all resolve to `https://inayaz-group.vercel.app`); lint/type-check clean |
| 3 | Unconfirmed metrics shown publicly ("Pending confirmation") | `sanity/schemas/documents/siteSettings.ts`, `sanity/seed.mts`, `sanity/lib/adapters.ts`, `src/constants/metrics.ts`, `Metrics.tsx`, `AnimatedMetric.tsx` | Replaced `confirmed: boolean` with `status: draft\|verified\|published`; renders published-only (no dash, no "Pending confirmation" text anywhere); falls back to a plain confirmed-facts strip when fewer than 3 are published (today: just 1, "Years of Experience", computed dynamically not hardcoded) | ✅ | `e2e/metrics.spec.ts` rewritten, 3/3 passing; `sanity/lib/adapters.test.ts` `adaptMetrics` suite rewritten, passing; live Sanity dataset re-seeded |
| 4 | Unverifiable recognition claims (CBE/COOP) | new `sanity/schemas/documents/recognition.ts`, `RecognitionAndTrust.tsx`, `src/constants/recognition.ts` (type only now), `sanity/lib/queries.ts`, `sanity/lib/adapters.ts` | New Sanity-backed schema (draft/verified/published), published-only rendering, logo gated on `logoApproved`, `evidenceFile` never selected by any public query; CBE/COOP seeded `draft` (hidden entirely); heading switches to neutral "Credentials and Institutional Relationships" below 2 published items | ✅ | `e2e/recognition-and-trust.spec.ts` rewritten (now targets `/about`), 5/5 passing; only GC-1 renders publicly today, confirmed via live re-seed |
| 5 | Homepage too long / repetitive | `src/app/(marketing)/page.tsx`, `CompanyIntroduction.tsx`, `Sustainability.tsx`, new `HomeFinalCTA.tsx` | Removed `MissionVision`/`CoreValues`/`RecognitionAndTrust` (already full on `/about`); trimmed `CompanyIntroduction` to a 2-sentence purpose statement (dropped its own duplicate of `/about`'s meaning-of-INAYAZ quote and timeline); added a `variant="preview"` to `Sustainability` (3 of 7 topics + "Explore Sustainability" CTA); replaced the inline `<Contact />` (full form) with a compact `HomeFinalCTA` band; `Metrics` moved to directly follow the intro as a single "credibility summary" step. New order: Hero → Credibility Summary → Divisions → Projects → Process → Why Choose → Sustainability Preview → Final CTA → Footer | ✅ | 126 e2e tests green (122 Desktop Chromium + 4 Mobile Chrome), 55 Vitest tests green, lint/type-check clean |
| 6 | No dedicated Real Estate journey | `sanity/schemas/documents/project.ts` (+6 new optional fields), `sanity/lib/{types,queries,adapters}.ts`, `src/constants/projects.ts`, `ProjectHero.tsx`, `ProjectCard.tsx`, `src/constants/navigation.ts`, new `real-estate/page.tsx` + `real-estate/[slug]/page.tsx` + 7 new components, `ProjectJsonLd.tsx` (path override), `sitemap.ts` | Added `unitTypes`/`amenities`/`pricingNote`/`paymentPlanNote`/`brochure`/`salesContact` to the project schema; fixed `adaptProject()` to stop stripping `status`/`completionYear`/`builtArea`/`units`/`gallery`/`services` (previously dropped despite existing in Sanity/GROQ); `/projects/[slug]` and `/projects` now render these conditionally instead of a hardcoded "Pending confirmation"; new `/real-estate` listing (Property Type + Location + dynamically-derived Status filter) and `/real-estate/[slug]` detail (overview, unit types, amenities, pricing-or-"Contact for pricing", payment plan, brochure download, similar developments, schedule-visit CTA) reusing the same project documents — no parallel content type, no fabricated data | ✅ | `e2e/real-estate.spec.ts` new, 17/17 passing; `project-detail.spec.ts`/`projects.spec.ts` updated for the hide-vs-placeholder change, passing; `adapters.test.ts` extended with 6 new `adaptProject` cases; full suite 139/139 Desktop Chromium (1 confirmed flake re-verified) + 60/60 Vitest |
| 7 | Generic CTA strategy | new `TrackedLink`/`TrackedAnchor` (analytics), `track.ts`, `division-cta.ts`, `DivisionDetail.tsx`, `CinematicHero.tsx`, `PremiumHeader.tsx`, `MobileNavigation.tsx`, `Footer.tsx`, `HomeFinalCTA.tsx`, `Contact.tsx`, 6× `*CTA.tsx`, `RealEstateDetails.tsx` | Intent-based CTA copy per division ("Discuss a Construction Project", "Send a Trade Inquiry", "Discuss Supply Requirements", "Get a Rental Quote", "Plan Your Trip") replacing one generic label everywhere; every CTA/tel/mailto/brochure link now fires a named Vercel Analytics event via small client-leaf wrapper components, keeping parent sections as server components | ✅ | `e2e/group.spec.ts` updated for per-division hrefs/labels; full suite green |
| 8 | Contact experience | `ContactForm.tsx` (full rewrite), `src/lib/validations/contact.ts`, `sanity/schemas/documents/contactSubmission.ts`, `src/lib/services/{contact,email}.ts`, new `service-interests.ts` | Required-field indicators, accessible focus-managed validation summary (`shouldFocusError: false` to avoid RHF's competing auto-focus), duplicate-submission guard, Ethiopia phone placeholder, "We usually respond within one business day," conditional field groups (Construction: type/location/budget/**start date**; Real Estate: development/unit type/visit date/contact method — new distinct `real-estate` interest option; Rental: equipment/period/location/date), URL-based prefill (`?interest=`/`?development=`) and UTM/lead-source capture, all persisted to Sanity and included in the notification email | ✅ | `e2e/contact.spec.ts` rewritten, 11/11 passing; `contact.test.ts` (Zod) +4 new cases, all passing; `contact.test.ts` (server action) unaffected, passing |
| 9 | Brand/legal naming inconsistency | new `src/constants/company.ts`, `Footer.tsx`, `OrganizationJsonLd.tsx`, `ArticleJsonLd.tsx`, `layout.tsx` | Centralized identity config; `legalName` (confirmed per CLAUDE.md) used only in copyright/legal contexts | ✅ | `company.ts` created; Footer, OrganizationJsonLd, ArticleJsonLd, root layout wired |
| 10 | Navigation simplification | `src/constants/navigation.ts`, `Wordmark.tsx` | Removed the explicit "Home" entry (logo already routes home); `PRIMARY_CTA` label → "Discuss a Project" everywhere it's used (nav, mobile menu, footer, all `*CTA.tsx` sections); `Wordmark` now carries `aria-current="page"`/`aria-label` so the logo preserves the active-state semantics "Home" used to provide | ✅ | `navigation.spec.ts` rewritten for the logo-active-state pattern; 7 e2e specs' "Contact Us" assertions updated to "Discuss a Project"; full suite 148 e2e (144 Desktop Chromium + 4 Mobile Chrome) + 64 Vitest green |
| 11 | SEO beyond URL fix | `ProjectJsonLd.tsx` (new), `Breadcrumbs.tsx` usage on About/Group/Services/Projects/News/Sustainability | Add missing Project JSON-LD; extend breadcrumbs to remaining top-level pages | ✅ | 72 e2e tests across About/Group/Services/Projects/News/Sustainability/Project-detail/Article-detail all green with breadcrumbs added |
| 12 | Legal/trust pages | new `LegalPageLayout.tsx`, `/privacy`, `/terms`, `/cookies`, `/accessibility`, `Footer.tsx`, `sitemap.ts` | Site-specific content (not generic boilerplate) naming the real data flow — Sanity storage, Resend email, in-memory rate limiting, cookieless Vercel Analytics, Studio-only auth cookies; no cookie banner since nothing non-essential requires consent; all four linked from the footer and included in the sitemap | ✅ | `e2e/legal-pages.spec.ts` new, 8/8 passing; full suite 156 e2e (152 Desktop Chromium + 4 Mobile Chrome) + 64 Vitest green |
| 13 | Accessibility hardening | `MotionSection.tsx`, `CinematicHero.tsx`, `MaskRevealImage.tsx`, `ProjectShowcase.tsx`, `layout.tsx`, `globals.css`, `global-error.tsx` (new) | Added `data-motion-initial` to every element shipping a static `opacity-0`/inline `clip-path` initial state; `.no-js` default class on `<html>` removed by a render-blocking inline script the instant JS runs, with a CSS override keeping that content visible if it never does; `<noscript>` stylesheet as an independent second fallback; `ProcessScroller`'s desktop tree (which has no server-safe opacity gating at all — panels would overlap illegibly, not just hide) forced to the always-safe stacked mobile timeline under `.no-js`; added the missing root-layout error boundary | ✅ | New `e2e/no-js-fallback.spec.ts` (5/5, JS genuinely disabled via Playwright) empirically proves hero/section/gallery/process content stays visible; full production build succeeds with `global-error.tsx` in place |
| 14 | Performance & motion verification | `ProcessScroller.tsx`, `e2e/construction-process.spec.ts` | Confirmed via automated check (not just manual) that the pinned section contains zero focusable elements, so keyboard Tab cannot get stuck inside it; re-verified `error.tsx`'s existing `unstable_retry` prop against Next 16.2.10's actual internals (both `reset` and `unstable_retry` are genuinely provided — not a bug) | ✅ | New keyboard-safety test passing; full suite 162 e2e (158 Desktop Chromium + 4 Mobile Chrome) + 64 Vitest green after re-seed |
| 15 | Footer improvements | `Footer.tsx` | Brand/legal-name fix (Phase A); Real Estate link + "Home" removal automatic via shared `NAV_ITEMS` (Phases D/F); legal links row added (Phase G); social links remain hidden (still none confirmed, unchanged policy) | ✅ | Covered by `legal-pages.spec.ts`'s footer test and `homepage.spec.ts`'s footer test |
| 16 | Content quality review | `src/constants/projects.ts` | Audited all constants for "world-class"/"leading"/"premier"-style superlatives. Fixed the one exact match to the user's own worked example (Ameliyaz's "world-class engineering execution" → "disciplined engineering coordination"). Left CLAUDE.md's explicitly-verified client-provided copy untouched elsewhere (the Vision statement's "become a leading... business group" is aspirational future-tense phrasing appropriate to a vision statement, not a present-tense factual claim; "Stronger Together: Connecting Industry Leaders" is a verified tagline about the network, not a self-description) | ✅ | Confirmed via repo-wide grep for superlative/claim patterns; no test asserted the old text, so no test updates needed; re-seeded Sanity with the updated description |

## Phase I — Final Verification (all issues above, combined gate)

Full verification suite run clean, in this order, after all fixes above:

- `npm run lint` — clean (1 pre-existing benign React Compiler warning, unrelated to this work)
- `npm run type-check` — clean
- `npm run test` (Vitest) — 64/64 passing
- `npm run test:e2e` (Playwright, both projects) — 178/178 passing (174 Desktop Chromium + 4 Mobile Chrome), including two new dedicated guard suites added specifically for this final gate:
  - `e2e/no-js-fallback.spec.ts` (5 tests, JavaScript genuinely disabled via Playwright) — empirically proves hero, section, gallery, and construction-process content stays visible with no JS
  - `e2e/console-errors.spec.ts` (16 tests) — zero unexpected network failures or JS/console errors across every public page (one expected, environment-only exception documented and filtered: Vercel Analytics' script only resolves on Vercel's actual edge infrastructure, always 404s under local `next build && next start`, resolves correctly once deployed)
- `npm run build` — succeeds cleanly, all 37 routes generated (13 static/legal pages, 7 project details, 7 real-estate details, 1 article, plus API/Studio routes)
- Manual production-HTML spot-check (direct curl against the built server, not just automated assertions): zero `localhost`/`127.0.0.1` anywhere in homepage source; canonical/`og:url` correctly resolve to `https://inayaz-group.vercel.app`; zero "Pending confirmation" text anywhere on the homepage; zero CBE/COOP draft content leaking anywhere; `robots.txt` correctly disallows `/studio`/`/api`; `sitemap.xml` contains exactly 28 correct entries; `/studio` still loads (200) — confirms the root-layout changes (Phase H) didn't break Sanity Studio

**Status: ✅ All 16 numbered issues verified complete.**

## Notes

- Two decisions confirmed with the user before implementation: **Vercel Analytics** for CTA tracking, and CBE/COOP recognition **hidden entirely** (not shown with a pending caveat) until real evidence is uploaded and published in Sanity.
- No WhatsApp CTA is added anywhere — no confirmed business WhatsApp number exists. Flagged as an asset still needed, not fabricated.
- This file is updated as each phase completes; it is the live audit trail, not a duplicate of the implementation plan.
